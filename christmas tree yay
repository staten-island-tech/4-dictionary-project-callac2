

down_count = []
up_count = []
h = 0
count_increasing = 1
count_decreasing = 1
def length(h):
    for i in range(1,length(h)):
            if [i] > i + 1:
                count_increasing += 1
                up_count.append(count_increasing)
            elif [i] < i - 1:
                count_decreasing += 1
                down_count.append(count_decreasing)
    
length(4[1,3,4,2])
                 

