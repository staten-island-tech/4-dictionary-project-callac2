down_count = []
up_count = []
number = 0
count_increasing = 1
count_decreasing = 1
def trees(number,x):
    for i in range(1,len(x)):
            if x[i] < x[i + 1]:
                count_increasing += 1
                up_count.append(count_increasing)
            elif x[i] < x[i + 1]:
                count_decreasing += 1
                down_count.append(count_decreasing)
            if count_increasing > count_decreasing:
                 print(count_increasing)
            else:
                 print(count_increasing)

trees(4[1,3,4,2])
print(f"{number,count_increasing,count_decreasing}")
                 

